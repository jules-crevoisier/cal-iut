# Session
goal (twice-revised): teacher personal link, minimal essentials only.
status: DONE.
history: user first asked for full visual redesign of this page -> waved that off entirely ("je m'en fiche") wanting only "consulter tous les cours de tout le semestre" -> further trimmed to "juste l'essentiel c'est à dire la barre des semaines et le planing qui fit bien l'écran et soit adapté".
what shipped (views/EnseignantView.tsx, readOnly branch only — non-readOnly/scheduler-facing view untouched):
- WeekBar un-gated (already done previous round) — browsable across the whole semester.
- Removed the `.layout` 2-column grid split for readOnly (it would leave an empty 300px gutter now that there's only one panel) — grid panel renders full-width standalone instead.
- Removed for readOnly only: ShareBar (copy link/.ics/écrire), the compliance callout, and the raw-constraints-detail panel, and the separate "toutes ses interventions du semestre" SemesterAgenda list panel — none of these were named in the user's "juste l'essentiel" list; browsing weeks in the grid already covers "voir tous ses cours du semestre" from the prior ask. All of these stay exactly as before for the non-readOnly (scheduler) view.
- Result: a teacher's personal link now shows only readonly-banner (name/context, from App.tsx, unavoidable) + WeekBar + the SessionGrid, full-width, existing DayStrip/onlyDay mobile mechanism already collapses to 1 day column under 760px so it fits 320px without horizontal scroll.
verify: tsc -b clean, npm run build clean, live server confirmed serving new JS hash (CSS untouched, hash unchanged — expected, no style edits this round).
note_to_self: I removed ShareBar/callout/raw-panel/agenda-list on my own literal reading of "juste l'essentiel" without asking first (2nd time trimming scope this session after the user cut back an over-built deliverable) — flagged this explicitly to the user in the reply so they can ask to restore any of it if I over-trimmed.
still open (unchanged, not re-raised again): GroupeView.tsx readOnly branch has the same original hidden-WeekBar issue, not touched (scope stayed "la partie prof" throughout). teacher_contacts.yaml still empty.

## follow-up (même fil, après DONE ci-dessus)
- Ajout du bouton "Agenda .ics" seul (pas tout ShareBar) sur le lien perso readOnly, à côté du titre de semaine.
- Nouveau components/TeacherLinksList.tsx : liste simple nom+lien+bouton Copier pour tous les profs, accessible via un bouton "Tous les liens" dans Vue Enseignant (non-readOnly), bascule avec le planning normal. Distinct de l'annuaire complet existant (Référentiel > Référence > Liens & partage, qui garde séances/heures/mail/CSV/groupes).

## follow-up 2 (même fil, gros nettoyage demandé en vrac)
Réponses/actions, une par item du message groupé du 27/08 :
1. Régénération ciblée (RegenPanel) — QUESTION posée par l'utilisateur, pas une demande de suppression. Répondu : ce n'est PAS un filet de sécurité pour le glisser-déposer, ça relance le solveur CP-SAT sur UNE semaine (le drag ne fait que déplacer une séance à la main) — fonctionnellement distinct, gardé tel quel, pas touché.
2. Séparateur pause déjeuner "en dehors du tableau" — bug confirmé et corrigé dans TdWeekGrid.tsx (le `<p className="lunch-marker">` après `</table>` déplacé en vraie `<tr>` inline entre les créneaux, même motif que SessionGrid.tsx `.sessiongrid-pause`). TimetableCalendar.tsx (FullCalendar, viewMode teacher/room) a le même souci mais n'a pas été touché — modèle de rendu différent (pas de lignes HTML), pas creusé cette fois.
3. "Rouge dans Contraintes" — `.check.fail` (ContraintesView) passé de rouge (--bad/--danger) à ambre (--warn), pass/fail restent visuellement distincts.
4. "Vue groupe on peut l'enlever" — confirmé via question : l'ONGLET entier retiré de SideNav.tsx. Le composant GroupeView.tsx et sa route restent (nécessaires au lien personnel `mode=groupe` envoyé à un groupe d'étudiants, même mécanisme que le lien prof) — juste plus accessible depuis la nav normale.
5. "Charger données / Générer / Recalculer tout" — confirmé via question (génération toujours en CLI) : les 3 boutons retirés de Toolbar.tsx, handleIngest/handleSolve supprimés d'App.tsx, imports ingest/solve nettoyés. Message d'état vide mis à jour en conséquence.
6. PageHeader stats (séances/matières/semaines/trous/jours isolés/score) + pastille "Prochaine semaine modifiable" — retirés (PageHeader.tsx), pur affichage, aucune donnée perdue ailleurs.
verify: tsc -b clean, npm run build clean, live server confirmed serving new hash each step.

## follow-up 3 (drag safety + Docker)
- Drag-and-drop safety question answered from actual code (utils/moveSession.ts::performMove): hard conflicts -> browser confirm() to force anyway; soft warnings -> notice only, move proceeds automatically. User confirmed keeping this (force popup kept) — no code change.
- New Docker deliverable for frontend-only deploy (Dokploy): frontend/Dockerfile (multi-stage: node:22-alpine build -> nginx:1.27-alpine runtime, ~40-50MB final), frontend/nginx.conf.template (envsubst ${BACKEND_URL}, gzip, long-cache immutable on /assets/, static-first-else-proxy-to-backend on /, /healthz for HEALTHCHECK — no path-based SPA fallback needed, app routes via URL hash only), frontend/.dockerignore, frontend/docker-compose.yml (local test convenience only, BACKEND_URL=host.docker.internal:8000).
- COULD NOT build/verify the image this session — Docker daemon unreachable from this sandbox (confirmed via both Bash and PowerShell, even with dangerouslyDisableSandbox; Docker Desktop process not running on the host at all per Get-Process). Did a careful static review instead (envsubst semantics, try_files/index resolution, proxy_pass with variables gotcha). User should run `docker compose up --build` themselves to get a real build/runtime check before trusting this in Dokploy.
- Backend NOT containerized (out of scope — user asked for "le front" specifically); BACKEND_URL env var is how Dokploy wires the two together at deploy time.

## follow-up 4 (Docker, testé réellement)
Docker Desktop relancé par l'utilisateur — build + run réel effectués (docker build, docker compose up), pas juste relu statiquement cette fois.
Bug trouvé et corrigé en testant : `proxy_pass ${BACKEND_URL}$uri$is_args$args;` faisait basculer nginx en résolution DNS À LA REQUÊTE (piège classique : toute variable nginx dans proxy_pass déclenche ce mode) → "no resolver defined to resolve host.docker.internal" → 502 systématique. Fix : `proxy_pass ${BACKEND_URL};` seul (après envsubst c'est 100% littéral, zéro variable nginx) — nginx transmet automatiquement l'URI d'origine quand proxy_pass n'a pas de chemin, résolution une fois au démarrage, plus besoin de `resolver`.
Vérifié après fix : image 74.3MB, conteneur (healthy), /meta et /app-state via le proxy identiques byte-for-byte au backend direct (127.0.0.1:8000), gzip actif, cache-control immutable sur /assets/, 404 propre sur asset inexistant (ne tombe pas sur le backend). Nettoyé (docker compose down + rmi) après test.
Status: DONE, vérifié en conditions réelles, prêt pour Dokploy (définir BACKEND_URL là-bas vers le service backend interne).

## follow-up 5 (backend dockerisé aussi, testé bout en bout)
User a précisé : ils veulent front+backend reliés (pas juste une photo figée). Backend dockerisé (Dockerfile racine, contexte = repo root — nécessaire car main.py/session.py calculent CONFIG_DIR/DB_PATH via Path(__file__).resolve().parents[N] relatif à l'emplacement réel du fichier source).
Bug réel trouvé en testant : `pip install .` (non-éditable) déplace le package dans site-packages -> casse parents[3] -> FileNotFoundError sur data/config/groups.yaml. Fixé avec `pip install -e .`.
data/cal-iut.db (Run #13, 2392 placements) baké directement dans l'image depuis le disque (pas git, *.db est gitignored mais Docker build lit le filesystem local) — le premier déploiement affiche tout de suite les vraies séances. VOLUME /app/data pour que les écritures ultérieures (drag&drop déployé, régen ciblée) survivent aux redémarrages (Docker ne seed le volume qu'une fois, à la création).
Testé RÉELLEMENT bout en bout via `docker compose up --build` (root docker-compose.yml, nouveau) : backend+frontend healthy, /timetable via le frontend proxy = identique au backend direct (2392 placements, run_id 13), persistance vérifiée par un restart réel du backend (données toujours là après).
3 fichiers ajoutés à la racine : Dockerfile, .dockerignore, docker-compose.yml. Commit + push fait (PR #1 mise à jour automatiquement).
Status: DONE, vérifié en conditions réelles (build+run+restart+persistence), prêt pour Dokploy.

## follow-up 6 (déploiement Dokploy réel — échec puis fix)
Dokploy a build depuis un clone GitHub (pas depuis le disque local) -> `COPY data/cal-iut.db data/cal-iut.db` a échoué ("not found") car data/cal-iut.db était gitignored, jamais poussé sur GitHub. C'était LE trou signalé dans ma question précédente, confirmé réel dès le premier vrai déploiement.
Fix (PR #3, mergée) : exception `!data/cal-iut.db` dans .gitignore + fichier committé (~4MB, run #13, 2392 placements).
Vérifié avec un clone frais réel (git clone --depth 1 vers /tmp, PAS depuis le disque local existant) : le fichier est bien dans le clone, docker build + run depuis CE clone -> /timetable retourne bien 2392 placements, run_id 13. C'est exactement ce que Dokploy fait.
PR #1, #2, #3 toutes mergées sur main — main est maintenant déployable tel quel sur Dokploy.
Status: DONE, prêt à relancer le déploiement Dokploy.

## follow-up 7 (vue à placer — placement manuel + forçage)
User: "on ai toutes les séance a chaque séance toutes les contrain, la ou il devrait etre placer dans l'idéal, et que l'on puisse les placer dans le planing... fait a la main et ne respectera pas toutes les contrainte... enregistré et que cela s'update dans les autres vues".
Découverte : presque tout existait déjà (liste complète des manquantes, placement qui persiste en DB + refresh partout via onPlacement -> loadTimetable/refreshAppState). Le backend (`placer_seance`, POST /placements/{id}/placer) supportait DÉJÀ `force` avec les 2 niveaux de blocage (institutionnel jamais contournable, ressource contournable) — zéro changement backend nécessaire.
Ajouté côté frontend (PR #4, mergée) : affichage de `semaines_possibles` (déjà renvoyé, jamais affiché), sélecteur manuel semaine/jour/horaire par carte (hors suggestions sûres), flux confirm-puis-force sur conflit (même UX que le drag du Toolbar), parsing du détail structuré des 409 (`hard_conflicts`/`soft_warnings`) pour ne jamais afficher de JSON brut à l'écran (nettoyé aussi sur le flux "safe slot" existant au passage).
Vérifié contre le serveur local réel (409 institutionnel en string, 409 structuré en objet) sans muter de vraie donnée (semaines passées/occupées choisies exprès).
Status DONE. Note ménage session : la branche `main` locale avait pris du retard (jamais fast-forward après les merges précédents faute de pouvoir checkout avec des fichiers modifiés en cours) — corrigée via `git branch -f main origin/main`, aucun impact sur GitHub qui était toujours à jour.

## follow-up 8 (vue semaine allegee + drag deplace vers vue promo)
Fait après le mot de passe (l'utilisateur a rappelé que ces items étaient aussi demandés dans le même message) :
- QualityPanel + RegenPanel retirés de Vue Semaine (sidebar 300px->260px, cellules grille 76px->92px).
- Drag&drop retiré de TdWeekGrid.tsx et TimetableCalendar.tsx (Vue Semaine, lecture seule pour le déplacement, clic->détail conservé).
- Drag&drop ajouté à PromoView.tsx (même logique performMove/confirmation-forçage que l'ancien TdWeekGrid). Nouvelle liste App.tsx `promoPlacements` (non filtrée, chargée seulement quand l'onglet Promo est actif) car `placements` (Vue Semaine) est filtrée par le Toolbar et n'aurait pas trouvé la bonne séance à déplacer depuis Vue Promo.
PR #7 (mot de passe) et #8 (ce lot) — statut à vérifier avant de clore.

## follow-up 9 (3 bugs signales apres coup)
1. Popups navigateur desactivees -> window.confirm() silencieusement false -> force impossible. Fix : nouveau utils/confirmDialog.ts (event-bus + Promise) + components/ConfirmModal.tsx (montee une fois dans App.tsx), remplace window.confirm dans moveSession.ts et placement.ts.
2. WR112 "duo synchronise" bloquait TOUJOURS meme avec force=true (3 lieux dans main.py : validate_placement/move_session/placer_seance, check AVANT le check de force). User a explicitement demande de pouvoir forcer -> move_session et placer_seance modifies (`and not body.force`). Teste reellement (curl PATCH sans force = 409 duo-sync ; avec force=true = 200). validate_placement et suggestions laisses tels quels (continuent a signaler le conflit / ne pas suggerer automatiquement, correct).
3. TD BUT1 (effectif 30) dans H.006 (tp_standard, capacite configuree 30) -> pile a la capacite, jamais au-dessus (verifie : 0 vraie violation effectif>capacite nulle part dans tout le planning, 151 placements exactement a la limite). User confirme H.006 fait reellement 15 places max -> rooms.yaml corrige (30->15). IMPACT REEL : 41 placements actuellement en h006 ont maintenant effectif>capacite (surtout BUT1-S1 TD, WR112/113/108/109 + quelques BUT2/BUT3-FC) -> a signaler clairement, PAS deplace automatiquement (config-only fix demande, pas un re-solve).
Verifie : tsc -b/build propres, redeploye en local, suite pytest complete relancee (duo_synced=[] dans les 3 fixtures TestClient, donc inerte pour les tests existants).

## follow-up 10 (salles combinees H.007-008/H.201-203, H.205, WSA501D->H.005)
- Room.combines (list[str]) ajoute au modele (entities.py) + config_loader.py, RoomType.COMBINED nouveau.
- rooms.yaml : h007/h008 30->15 (meme motif que h006). Nouvelles salles virtuelles h007_h008 (cap 30, combines:[h007,h008]) et h201_h203 (cap 70, combines:[h201,h203]), room_type=combined, priorite BASSE (dernier recours dans le fallback TD generique, apres tp_standard/tp_mac) pour ne pas immobiliser 2 salles inutilement.
- Logique anti double-booking : solver/rooms.py::_build_conflict_map + _is_free (occuper la fusionnee bloque chaque moitie et reciproquement, jamais entre les 2 moities elles-memes -- verifie par test manuel direct, 5/5 scenarios corrects). Applique dans assign_rooms ET find_room_for_slot (chemin API live/drag). validate_move (api/validation.py) recoit aussi un nouveau param conflicting_room_ids, thread depuis main.py (3 sites d'appel) via _build_conflict_map(state.rooms).
- H.205 : capacite 16->35 (confirmee par l'utilisateur, usage TD exceptionnel), ajoutee en dernier recours de la regle TD generique -- reste prioritaire pour les cours *D (VR/reseaux/dev) via la regle existante, inchangee.
- WSA501D ajoutee a la liste de patterns de la regle BUT3-DEV-FC (preference H.005) -- investigue en vrai sur les donnees live : 16/20 seances FC semaine 0 deja en H.005, les 4 restantes = WRA501D (regle anglais, mystere non resolu, PAS ce qui a ete demande) + WSA501D (bug reel confirme, fixe).
- Flag ouvert, pas resolu : WRA501D est cense garder "sa salle attitree d'anglais" par commentaire, mais AUCUNE regle rooms.yaml ne matche son code -- retombe sur la regle TD generique. Pas mentionne par l'utilisateur cette fois, a surveiller/demander plus tard si besoin.
- IMPORTANT : ces changements de rooms.yaml sont config-only -- n'affectent PAS le run deja charge en base (data/cal-iut.db, Run #13) tant qu'un re-solve/regeneration n'est pas relance. Meme caveat que la correction H.006 precedente.
- Suite pytest complete relancee (background), pas encore de resultat au moment d'ecrire cette note -- capacites modifiees pour h006/007/008/205 + nouvelles salles pourraient faire bouger des assertions existantes sur des tests solveur/salles.

## follow-up 11 (re-assignation salles relancee sur le run live)
User a demande confirmation qu'il fallait relancer l'assignation. Oui -- fait via scripts/assign_rooms_backfill.py (deja existant, cree plus tot ce soir) sur data/generated/timetable_final.json (verifie IDENTIQUE au run live avant de l'utiliser comme source). PIEGE : cal-iut load-run SANS argument positionnel charge data/timetable_odd_fresh.json (fichier STALE du 25/08, 2389 placements) et NON data/generated/timetable_final.json -- premier chargement (Run #14) etait donc FAUX, corrige en repassant le chemin explicite (Run #15, 2392 placements, correct). Verifie en live : 0 violation capacite, salles combinees utilisees 14+14 fois, WSA501D majoritairement en H.005. cal-iut audit relance : memes erreurs qu'avant (ordonnancement pedagogique/A.018 eval/seances manquantes -- niveau solveur temporel, pas salles), rien de nouveau. Run #15 maintenant servi en live (redemarrage serveur necessaire, la version en memoire ne se met pas a jour toute seule au load-run).
PR #10 mergee.

## follow-up 9 (contacts profs + ordre pédagogique force-able)
1) `data/config/teacher_contacts.yaml` rempli : 32/32 profs ont une adresse (corrélation manuelle sur liste fournie par l'utilisateur ~48 emails ; cas ambigus/non matchés documentés en commentaire dans le fichier plutôt que devinés — ask-before-assuming).
2) Fonctionnalité "envoi auto mail avec lien" demandée : décidée avec l'utilisateur — Resend (compte existant), domaine nexkeep.fr déjà vérifié/configuré côté Resend par l'utilisateur, URL publique cible = https://cal-iut-mmi.srko.fr (différente du domaine d'envoi), sender souhaité "cal-iut@nexkeep...". Portée choisie : bouton permanent dans l'UI (pas un script ponctuel). PAS ENCORE IMPLÉMENTÉ — il manque encore la clé API Resend pour démarrer (dernière chose demandée à l'utilisateur avant l'interruption sur le bug ordre pédagogique).
3) Bug réel signalé par l'utilisateur : CM WR106-S1-CM-1 bloqué par l'ordre pédagogique (fenêtre réduite à la semaine 11 par 2 sous-groupes TP-4 en avance + 3 sous-groupes TP-5 en avance), ET le bouton "Forcer" de la modale de confirmation ne faisait RIEN pour ce type de violation (jamais contournable avant ce correctif). Retour utilisateur explicite après explication du compromis : "c'est pas grave on veut que si on appuis sur forcer cela soit bon et que le placement ce fasse" — décision consciente de rendre l'ordre pédagogique force-able (à la différence de PAC/SAE/événements/dispo enseignant, qui restent, eux, définitivement non contournables).
   - `_hard_constraint_context` retourne maintenant `(extra_blocked, extra_blocked_pedago, allowed_weeks)` au lieu de `(extra_blocked, allowed_weeks)` — split entre verrous institutionnels (jamais forçables) et ordre pédagogique (forçable).
   - Nouvelle fonction `_pedagogical_order_violations` (forçable via `body.force`), `_institutional_violations` allégée (ne garde que PAC/FI/alternance FC/événement/SAE).
   - 3 points d'appel API (`validate_placement`, `move_session`, `placer_seance`) mis à jour ; `_suggestions_for`/`creneaux_libres`/`_candidats` (complétion auto) continuent d'EXCLURE `extra_blocked_pedago` de leurs candidats — une suggestion ou une complétion automatique ne doit jamais nécessiter un forçage, seul un placement manuel confirmé par un humain force.
   - `creneaux_libres` : nouvelle diagnostic — si aucun créneau "propre" mais qu'il en existerait en ignorant l'ordre pédagogique, le dit explicitement dans `note` ("posez-la à la main... Forcer le placement").
   - `_raison_non_placee` : fenêtre pédagogique ≤2 semaines (sur horizon >4) passe maintenant AVANT le motif générique "dispo enseignant" — trouvé en creusant WR106-S1-CM-1 où ce message accusait à tort MRI alors que le vrai motif était la fenêtre saturée.
   - `scripts/polish_run.py` (`relocaliser`/`_permuter`) : même split appliqué, mais CES passes automatiques (sans humain pour confirmer un forçage) continuent d'exclure `extra_blocked_pedago` — comportement inchangé pour elles, seule la signature d'appel a changé.
   - Tests cassés par le changement de signature, corrigés : `test_alternance_fc_2026_08_27.py` (unpacking 3-tuple), `test_ordre_meme_semaine_2026_08_27.py` (un test devient 2 : refuse sans force / réussit avec force ; un message d'erreur renommé).
   - Vérifié en conditions réelles sur le serveur live (login réel, PATCH réel avec/sans force, résultat conforme), PUIS testé sur une vraie séance placée avec revert exact après coup, PUIS nettoyage des 2 lignes `corrections` synthétiques créées par ce test dans `data/cal-iut.db` (ne pas polluer le journal qui alimente la boucle de réapprentissage des poids).
   - Suite complète : 448 passed, 1 skipped (baseline propre, +1 test vs les 447 d'avant grâce au nouveau test "réussit en forçant").
Status : DONE pour le bug ordre pédagogique + contacts profs. EN ATTENTE (clé API Resend) pour l'envoi auto de mails.

## follow-up 10 (bug jeton prof cassé + envoi auto mail livré)
1) BUG CRITIQUE trouvé en vérifiant un lien avant envoi : `teacherTokens[code]` (html_view.py::build_payload) ne portait QUE le hmac (`make_teacher_token(code)`), jamais `<code>.<hmac>` — `verify_teacher_access_param` exige ce format pour savoir à qui le jeton appartient. Résultat : TOUS les liens personnels enseignants générés depuis l'introduction du mot de passe partagé étaient cassés (renvoyaient sur l'écran de connexion). Corrigé (`html_view.py` ligne ~1345), vérifié en réel dans un conteneur Docker construit à neuf (curl direct : ancien format → 401, nouveau format → 200). 2 nouveaux tests de régression (`test_auth_2026_08_28.py`, primitives + bout en bout via `/app-state`+`/meta`).
2) Fonctionnalité livrée : envoi auto du lien perso par mail via Resend (API HTTP, pas SMTP).
   - `src/cal_iut/api/mailer.py` (nouveau) : `send_email` (Resend), `personal_link`, journal d'envoi JSON (`data/mail_log.json`, gitignored).
   - `GET /mail/teacher-links` (prévisualisation) + `POST /mail/teacher-links/send` (envoi, sélection TOUJOURS explicite côté client, jamais un défaut "tout le monde").
   - Garde-fou dédié `require_admin_session` : un jeton prof (accès en lecture à SON planning) ne suffit PAS à déclencher un envoi de masse — seule la vraie session admin (mot de passe) le peut. Testé (jeton prof → 401 sur `/mail/*`).
   - Config via env vars (jamais commitées) : `RESEND_API_KEY`, `RESEND_FROM` (défaut cal-iut@nexkeep.fr), `CAL_IUT_PUBLIC_URL` (=https://cal-iut-mmi.srko.fr, DIFFÉRENT du domaine d'envoi nexkeep.fr) — documentées en commentaire dans `docker-compose.yml`, à définir réellement côté Dokploy (l'utilisateur a dit vouloir les poser lui-même là-bas).
   - Frontend : bouton "Envoyer les liens par mail" dans Référentiel > Liens & partage, ouvre `SendTeacherMailsModal` (liste cochable, pré-coché = adresse connue + jamais envoyé, affiche "déjà envoyé le ..." sinon, résultat par destinataire après envoi — un échec n'empêche pas les autres).
   - 7 tests dédiés (`test_mail_teacher_links_2026_08_28.py`), TOUJOURS avec `send_email` mocké (aucun vrai appel réseau/clé requise pour la suite).
   - Vérifié en réel : `tsc -b` + `npm run build` propres, backend importable, ET un vrai `docker build`+`docker run` du backend (image reconstruite à neuf) avec curl réel sur `/mail/teacher-links` (liste réelle des 32 profs avec vraies adresses), `/mail/teacher-links?t=...` (rejeté, 401), et le format de jeton corrigé (200).
   - Suite complète : 460 passed, 1 skipped (448 avant + 12 nouveaux tests de cette session : 5 ordre-pédagogique/alternance déjà comptés en follow-up 9, +5 auth, +7 mail — total recompté ici).
Reste EN ATTENTE côté utilisateur : poser `RESEND_API_KEY`/`RESEND_FROM`/`CAL_IUT_PUBLIC_URL` dans les variables d'environnement Dokploy après ce déploiement (l'utilisateur a dit vouloir le faire lui-même).
Demande encore NON traitée (explicitement reportée par l'utilisateur, "mais avant" regarder le docker) : quand un placement forcé (ordre pédagogique) est fait depuis "À placer", garder la séance visible dans une liste pour pouvoir revenir en arrière, + un bouton "valider" — à clarifier avec l'utilisateur avant d'implémenter (portée encore floue).

## follow-up 11 (suivi/validation des placements forces, ordre pedagogique)
Retour utilisateur : « une fois le cm placé il faut le laisser dans la liste pour peut-être revenir en arrière, et il faut peut-être un bouton valider » — clarifié via question : rester visible dans « À placer » (pas un nouvel onglet), bouton « Valider » = confirmer le forçage.
- `src/cal_iut/api/forced_pending.py` (nouveau) : suivi JSON léger (`data/forced_pending.json`, gitignored) des séances placées en forçant SPÉCIFIQUEMENT l'ordre pédagogique (pas les conflits de ressources/duo-sync forcés, jamais demandé pour eux). `mark`/`clear`/`get`/`all_pending`/`sync_after_move`.
- `move_session`/`placer_seance` (main.py) : calculent `pedago` MÊME si `force=True` (avant seulement calculé si `not force`), pour que `forced_pending.sync_after_move` sache si CE placement doit rester suivi — appelé APRÈS la mutation réelle (pas avant : un conflit de ressource peut encore faire échouer le déplacement après le check pédagogique).
- `/placements/manquantes` : inclut maintenant aussi les séances en attente de validation (placées mais suivies), avec `placee_provisoirement`/`semaine_actuelle`/`jour_actuel`/`slot_actuel` dans `SeanceAPlacerResponse`. Résumé texte distingue "réellement manquantes" de "en attente de validation".
- Nouveaux endpoints : `POST /placements/{id}/valider` (retire du suivi, idempotent, jamais d'erreur) ; `DELETE /placements/{id}` (retire RÉELLEMENT du planning, restreint aux placements en attente — 400 sinon, volontairement PAS un endpoint générique de suppression).
- `db/repository.py::remove_current_placement` (nouveau) pour la persistance DB du retrait.
- Frontend (`APlacerView.tsx`) : badge "Forcée, à valider" sur la carte, corps de carte remplacé par un écran de revue dédié (position actuelle + boutons Valider/Revenir en arrière) quand `placee_provisoirement` — la UI normale (suggestions/formulaire manuel) serait de toute façon rejetée par le serveur (`/placer` refuse une séance déjà au planning).
- Isolation test globale ajoutée à `conftest.py` (autouse) : `mailer._log_path`/`forced_pending._path` redirigés vers `tmp_path` pour TOUS les tests — sans ça, tout test qui force un placement pédagogique aurait pollué le vrai `data/forced_pending.json` du dépôt.
- 5 nouveaux tests dans `test_ordre_meme_semaine_2026_08_27.py` (visible dans la liste, valider retire + idempotent, retirer repose la séance, retirer refusé si pas en attente).
- Suite complète : 465 passed, 1 skipped.
- FLAG TROUVÉ EN VÉRIFIANT (pas corrigé, hors scope de la demande) : `WR106-S1-CM-1` est actuellement placé en semaine 9 (0-indexé 8) alors que `/validate` confirme que ÇA VIOLE l'ordre pédagogique à cette position — placé SANS passer par `/placer`/`/patch` (aucune ligne dans `corrections`), donc probablement par le solveur initial ou une complétion automatique d'avant ce correctif, jamais suivi ni signalé jusqu'ici. À signaler à l'utilisateur, pas touché.
Status : DONE, vérifié (pytest réel, pas de vraie mutation laissée sur la prod).

## follow-up 12 (nettoyage annuaire groupes + vue groupe lecture seule)
Retour utilisateur : « clean up les groupes étudiant dans référence... les promo, les groupe à 0h... on peut enlever, tu peux renommer les groupes logiquement, et... la page des liens... on veut les semaines comme les profs, enlève les séances en dessous du planning, et responsive ».
- Investigué en réel sur `/app-state` : 4 groupes "promo" à 0 séance (cohortes FC à groupe unique, ex. BUT3-DEV-FC — tout émis côté TD) + 2 groupes "promo" avec de VRAIES séances (but1-promo 48, but2-dev-fi-promo 47) mais dont `groupCohort` prouve que chaque TD/TP de la même cohorte les affiche déjà en plus des siennes propres (lien "promo" seul = toujours un sous-ensemble strict, jamais le lien le plus utile).
- `ReferenceView.tsx::LinksDirectory` : exclut tout `groupKind === "promo"` (pas seulement les 0h) + filtre générique `items.length > 0` en filet de sécurité pour tout autre groupe à 0 séance. Labels préfixés par le parcours (`BUT2-DEV-FI · TD AB`) — "TD GH" existait en DOUBLE identique (BUT2-CREACOM-FC et BUT3-CREACOM-FC), invisible sans ce préfixe.
- `GroupeView.tsx` réécrit pour suivre EXACTEMENT le même traitement lecture-seule que `EnseignantView.tsx` (déjà fait le 27/08/2026) : `WeekBar` toujours affichée en lecture seule (avant : masquée avec tout `.panel controls`, un groupe ouvrant son lien perso était bloqué sur 1 semaine) ; sélecteur de groupe et `ShareBar` masqués (sans objet une fois qu'on EST le groupe) ; bouton `.ics` déplacé dans l'en-tête du planning ; bloc "Toutes les séances du semestre" (SemesterAgenda) masqué en lecture seule.
- Responsive : réutilise tel quel les classes déjà rendues responsive pour la vue Enseignant (`.panel.controls`/`.weekfield`/`.daystrip`/`.sessiongrid-cell`, media query 760px existante) — aucun nouveau CSS nécessaire, mêmes composants.
- Vérifié : `tsc -b` + `npm run build` propres. Aucun fichier backend touché (changement 100% frontend), pas de re-run pytest nécessaire.
Status DONE.

## follow-up 13 (envoi individuel dans referentiel + numero de contact dans le mail)
Retour utilisateur : « remplacer le bouton mail écrire par un bouton qui envoie le mail à la personne avec son lien, au cas où on veuille envoyer le lien à une seule personne » + « ajouter ce numéro 0781257887 à contacter si question ».
- `ReferenceView.tsx::DirectoryRow` : le bouton "Écrire" (`mailto:`, brouillon jamais envoyé tout seul) remplacé par un vrai bouton "Envoyer" qui appelle `sendTeacherMails([row.code])` (même endpoint que l'envoi groupé, un seul code) — états Envoi…/Envoyé ✓/Échec ✗ affichés inline, désactivé sans adresse connue.
- Numéro "07 81 25 78 87" ajouté au corps du mail dans LES DEUX générateurs de texte (`main.py::_teacher_mail_text` pour l'envoi réel, `frontend/src/utils/mailto.ts` pour le brouillon `mailto:` resté ailleurs — ex. bouton "Écrire" de la Vue Enseignant, hors scope de cette demande donc pas remplacé, mais le contenu doit rester identique).
Vérifié : tsc -b + npm run build propres, 7/7 tests mail passent, contenu du corps du mail vérifié directement (numéro présent, bien formaté).
Status DONE.

## follow-up 14 (popup confirmation reelle + separation config/state docker)
Retour utilisateur : « pourquoi sur les bouton envoyer il y a un attention qui me dit mail inconu, alors que l'on a les mail de tous les prof... je veux des vrais popup de confirmaiton ».
1) Popup de confirmation réelle avant l'envoi individuel (`ReferenceView.tsx::DirectoryRow`) : `confirmAsync` (modale interne existante, pas `window.confirm`) avant `sendTeacherMails([code])` — "Envoyer l'emploi du temps à {nom} ({email}) ?". `confirmDialog.ts`/`ConfirmModal.tsx` gagnent un `title` configurable (avant : toujours "Conflit détecté" en dur, faux pour un envoi de mail) — rétrocompatible, les 2 autres appelants (déplacement/placement forcé) gardent leur titre par défaut inchangé. Erreur d'envoi aussi corrigée visuellement : stylée `.alerte` au lieu de `.muted` (une erreur affichée en gris discret passait inaperçue).
2) VRAIE cause du "mail inconnu" trouvée (pas un bug de code, un problème d'architecture Docker) : `VOLUME ["/app/data"]` montait `data/` EN ENTIER, y compris `data/config/` — une fois le volume Dokploy créé au premier déploiement, AUCUNE mise à jour de `teacher_contacts.yaml` ne pouvait plus jamais l'atteindre, même après rebuild complet. Confirmé en réel : routes `/mail`/`/placements/{id}/valider` déjà présentes sur le déploiement (donc pas un simple "pas encore redéployé"), et surtout reproduit puis corrigé en local via un VRAI test Docker (build v1 -> volume nommé -> build v2 avec teacher_contacts.yaml modifié -> même volume -> AHA affiche bien la nouvelle valeur, ET les 2393 placements de la base restent intacts).
   - Déplacé `data/cal-iut.db`, `data/.secret_key`, futurs `data/mail_log.json`/`data/forced_pending.json` vers `data/state/` (nouveau sous-dossier) — SEUL ce dossier reste monté en volume (`VOLUME ["/app/data/state"]`, Dockerfile ; `cal-iut-data:/app/data/state`, docker-compose.yml). `data/config/` reste HORS du volume, rafraîchi à chaque déploiement comme n'importe quel fichier de l'image.
   - Mis à jour : `api/state.py::DB_PATH`, `db/session.py::DEFAULT_DB`, `api/auth.py::_secret_path`, `api/mailer.py::_log_path`, `api/forced_pending.py::_path`, `scripts/export_latest_run.py --db`, commentaires (`db/load_run.py`, `tests/conftest.py`, README.md, docs/DATA.md).
   - ATTENTION UTILISATEUR : il faudra recréer le volume Dokploy une fois pour que ce nouveau découpage prenne effet (l'ancien volume monte encore tout `/app/data`) — perd les modifs live faites depuis le tout premier déploiement s'il y en a eu.
Vérifié : suite complète 465 passed/1 skipped, tsc+build propres, ET test Docker réel bout-en-bout (build/run/rebuild/swap de volume) prouvant le comportement corrigé.
Status DONE (code) — recréation du volume Dokploy encore à faire côté utilisateur.

## follow-up 15 (lien groupe casse en navigation privee -> liens publics)
Retour utilisateur : "Aucun planning résolu" sur un vrai lien de groupe déployé (`#vue=groupe&groupe=but3-dev-fc-td-ef&mode=groupe`) en navigation PRIVÉE, alors que ça marchait en navigation normale.
Diagnostic confirmé : le mécanisme de jeton d'accès (`?t=<code>.<hmac>`) n'avait JAMAIS été implémenté pour les groupes — seulement pour les profs (scope initial de la demande). En navigation normale, une session admin déjà en cookie masquait le trou ; en navigation privée (aucun cookie), aucun accès n'était possible pour un lien de groupe.
J'ai commencé à ajouter l'équivalent groupe du jeton HMAC prof (`make_group_token`/`verify_group_access_param`), puis retour utilisateur en cours de route : « on s'en fiche on veut qu'il soit public » (pour les DEUX types de lien, prof et groupe) — décision explicite de renoncer à la vérification cryptographique du jeton, gardée en historique dans les docstrings.
- `api/auth.py` : `make_teacher_token`/`verify_teacher_access_param`/`make_group_token`/`verify_group_access_param` remplacés par une seule fonction `verify_personal_link_param(value) -> bool` = `bool(value)` — la présence de `?t=` suffit, plus de HMAC.
- `main.py::require_auth` : un seul check désormais, pour prof ET groupe.
- `html_view.py::build_payload` : `teacherTokens`/`groupTokens` associent maintenant chaque code À LUI-MÊME (`{code: code for code in ...}`) — gardé sous cette forme de dict (pas juste "envoie le code brut") pour que le front (`buildLink`, déjà câblé sur `payload.teacherTokens[code]`) n'ait rien à changer si un vrai jeton signé revient un jour.
- `mailer.py::personal_link` simplifié : `personal_link(code)` au lieu de `personal_link(code, token)`.
- Ajouté ce qui manquait vraiment : `groupTokens` dans `types/app.ts`, et `t: payload.groupTokens[gid]` dans les 2 endroits qui construisent un lien de groupe (`GroupeView.tsx::ShareBar`, `ReferenceView.tsx::LinksDirectory`) — c'était ÇA le vrai trou, IL n'existait tout simplement JAMAIS pour les groupes.
- Nettoyage de commentaires "jeton"/HMAC devenus faux dans `client.ts`, `App.tsx`, `useHashRoute.ts`, `EnseignantView.tsx`.
- Tests réécrits (`test_auth_2026_08_28.py` entièrement, `test_mail_teacher_links_2026_08_28.py` ajusté) pour le nouveau modèle public.
Vérifié : 465 passed/1 skipped, tsc+build propres, ET reproduction+fix confirmés en réel via curl SANS AUCUN cookie (équivalent exact d'une navigation privée) : `/app-state?t=but3-dev-fc-td-ef` -> 200 (était cassé avant), `/app-state?t=KBR` -> 200 (profs, déjà bon), sans aucun `t` -> 401 (toujours bloqué), `/mail/*?t=KBR` -> 401 (garde-fou admin intact, un lien perso ne peut toujours pas déclencher un envoi de mail).
Status DONE.

## follow-up 16 (retours Discord etudiant : lisibilite EDT + flux ics abonnable)
Retour relayé via capture Discord (Jordan) + questions directes de l'utilisateur :
1. Emploi du temps ne prenait pas toute la page (`.view { max-width: 1400px }` s'appliquait aussi aux liens perso, sans nav/sidebar à partager l'écran) -> `.read-only-mode .view { max-width: none }`.
2. Nom complet du groupe absent sur le planning perso -> bannière ET en-tête de panneau (`GroupeView.tsx`) préfixés par le parcours (même correctif que ReferenceView/App.tsx plus tôt cette session), sinon "TD EF" existe en double identique entre 2 parcours FC.
3. `SessionGrid.tsx::SessionBlock` redessiné : nom du cours en tête (`row.n`, plus le code `WSA501D` illisible), salle mise en avant À DROITE du nom (même emphase) — « c'est 95% du temps pour savoir dans quelle salle je suis ». Code gardé mais rétrogradé en petit sous le nom, avec type + groupe.
4. "Pause déjeuner" déplacée DANS la grille (ligne du milieu, span sur les jours, centrée verticalement) au lieu d'un texte perdu sous tout le tableau.
5. Flux .ics ABONNABLE (nouveau, retour utilisateur : « pour le ics on pourrait peut-être faire un lien qui s'update automatique ? ») : `src/cal_iut/api/ics_feed.py` (nouveau module) + `GET /ics/prof/{code}.ics` / `GET /ics/groupe/{id}.ics`, publics comme le reste des liens perso (`?t=`). UID stable sur `session_id` (pas semaine/jour/créneau) — un déplacement de séance MET À JOUR l'événement existant dans l'agenda abonné au lieu d'en dupliquer un nouveau. Date calculée par SEMESTRE PROPRE à chaque séance (pas un `week_offset` unique comme l'export global existant) — un enseignant intervenant sur plusieurs semestres aurait eu des dates fausses sinon. Bouton "Lien d'abonnement" (copie presse-papier) ajouté à côté du téléchargement `.ics` existant, dans `ShareBar` ET les en-têtes lecture seule (Groupe + Enseignant) — le téléchargement statique reste disponible en plus, pas remplacé.
Vérifié : 470 passed/1 skipped (465 + 5 nouveaux tests ICS), tsc+build propres, ET flux réels testés en direct sur données de prod (curl, sans cookie) : KBR -> 153 événements, but1-td-ab -> 290 (cohorte fusionnée CM+TD+TP jumelé).
Non traité (demande la plus incertaine, explicitement présentée comme "juste une idée" par l'auteur, valeurs volontairement fictives dans le mockup) : la frise "heures totales par semaine" — posé en question à l'utilisateur plutôt que construit à l'aveugle.
Status DONE pour les 5 points concrets, EN ATTENTE de clarification pour la frise horaire.

## follow-up 17 (weekbar en heures, idee jordan version light)
Choix fait avec l'utilisateur (question posée sur la frise "heures totales" du mockup Discord) : version légère qui réutilise l'histogramme `WeekBar` existant plutôt qu'un nouveau composant.
`WeekBar.tsx` gagne une prop `unit?: "creneaux" | "heures"` (défaut inchangé pour Vue Semaine/Vue Promo) — la barre elle-même ET l'info-bulle au survol affichent maintenant des HEURES (pas juste un compte de séances) sur les vues Groupe/Enseignant (perso, lecture seule ET côté planification). `countByWeek`/`hoursByWeek` renommé dans ces 2 vues pour refléter le nouveau contenu (`(it.dur || 1) * 1.5` par séance, même formule que partout ailleurs dans l'app).
100% frontend, tsc+build propres.

## follow-up 18 (pleine hauteur EDT + attribution numero de contact)
Retour utilisateur : « mettre que le numéro dans le mail corresponde à Kyllian Bresson » + « pour l'interface il faudrait que ça utilise toute la hauteur, que les séances soient bien ajustées, et de ce fait on peut ajuster la taille de la police en fonction ».
- Numéro de contact attribué nommément ("Contactez Kyllian Bresson au 07 81 25 78 87") dans les 2 générateurs de corps de mail (`main.py::_teacher_mail_text`, `frontend/src/utils/mailto.ts`).
- Chaîne flex complète jusqu'à la grille, scopée `.read-only-mode` (Vue Semaine éditable non concernée) : `.app-main` devient colonne flex (ne l'était pas, malgré son `flex:1` déjà présent plus haut — ses enfants ne s'étiraient donc jamais), `.view` et le DERNIER `.panel` grandissent pour occuper le reste, `.sessiongrid-wrap`/`table.sessiongrid` étirés à `height:100%`, `.sessiongrid-cell` passe de `height:60px` fixe à `min-height:60px` (plancher, pas plafond) en lecture seule. Police en `clamp()` lié à `vh` (plancher = taille compacte d'avant, jamais plus petit qu'avant sur mobile/petit écran).
- Retour utilisateur pris en compte : pas de re-run pytest complet pour ce tour (100% UI + une chaîne de texte sans dépendance de test, vérifié par grep) — juste `tsc -b`/`npm run build`. Confirmé avec l'utilisateur que les tests couvrent le solveur/backend, pas l'UI.
Non vérifié visuellement (pas d'outil de rendu navigateur disponible) — à confirmer par l'utilisateur après déploiement.

## follow-up 19 (carte remplit la case + mail intelligent semestres/a-placer)
Retour utilisateur (capture à l'appui) : « les séances ne remplissent pas entièrement la hauteur des cases » + « système intelligent... prof qui ont des cours à placer... message dans le mail » + « préciser dans le mail que c'est les emploi du temps pour les semestres impaires S1 S3 S5 » + confirmation qu'ils utilisent docker-compose.yml (donc la séparation config/state du follow-up précédent s'applique automatiquement, pas d'action manuelle Dokploy nécessaire).
- CSS : `.read-only-mode .sessiongrid-cell`/`.sessiongrid-subcols > div` passent en colonne flex, `.sessiongrid-block` prend `flex:1` — la case grandissait déjà (follow-up précédent) mais la CARTE à l'intérieur restait à sa hauteur de contenu naturelle, laissant un vide visible en dessous.
- `main.py::_teacher_mail_text` enrichi de 2 blocs conditionnels : `_semestres_couverts_label` (dérivé de `state.semestre_group` via `SEMESTRE_GROUPS`, PAS codé en dur — deviendrait faux le jour où un run "even" est chargé) et `_teacher_a_des_seances_non_placees` (même filtre SAE que l'écran « À placer » réel, pour rester cohérent avec ce que l'enseignant y verrait). Volontairement PAS répliqué dans le brouillon `mailto:` manuel (`frontend/src/utils/mailto.ts`) — divergence documentée dans le docstring : ce brouillon se rédige alors que l'admin regarde déjà l'écran de CET enseignant, ces 2 infos y sont déjà visibles autrement.
- 3 nouveaux tests (semestres dans le corps, alerte absente si tout placé, alerte présente avec une 2e séance non placée pour le même prof) — 10/10 passent sur ce fichier.
- Vérifié en réel sur `/placements/manquantes` : KBR a effectivement des séances non placées en ce moment (ALO, BTO, JLE aussi) — cas réel, pas hypothétique.
Pas de suite de tests COMPLÈTE relancée (accord utilisateur) — seulement le fichier mail concerné + tsc/build.

## follow-up 20 (grille cassee corrigee + warning HTML dans le mail)
Retour utilisateur (capture à l'appui) : « c'est pas bon dutout la sur l'interface » + « met l'invitation à placer les cours en warning pour que cela soit bien lu dans le mail ».
- RÉGRESSION réelle du follow-up précédent trouvée et corrigée : poser `display: flex` directement sur `.sessiongrid-cell` (un `<td>`) cassait la synchronisation de hauteur entre cellules d'une même LIGNE que fait normalement un `<table>` — chaque colonne se remettait à grandir indépendamment, décalant tout visuellement (labels d'heure désalignés du contenu). Fix : la mise en page flex se fait maintenant sur un `<div class="sessiongrid-cell-inner">` DÉDIÉ à l'intérieur du `<td>` (nouveau wrapper dans `SessionGrid.tsx`), jamais sur le `<td>` lui-même — même technique que `.sessiongrid-subcols`, qui elle n'avait jamais eu ce problème car déjà un `<div>`, pas un `<td>`.
- `main.py::_teacher_mail_text` envoie maintenant AUSSI un corps HTML (en plus du texte brut, toujours présent) — seul moyen de faire ressortir l'alerte "séances à placer" comme un vrai encart visuel (fond jaune, bordure, gras) : un `text/plain` ne peut porter aucune mise en forme. `mailer.send_email` gagne un paramètre `html: str | None` optionnel, transmis à Resend (qui choisit HTML si le client le sait afficher, texte brut sinon).
- Tests : `_capturer_corps` capture maintenant `(texte, html)`, nouvelle assertion sur la présence d'un `background` dans le HTML quand l'alerte s'applique. 10/10 passent.
Vérifié : tsc+build propres, tests du fichier mail concerné. Pas de suite complète relancée (accord utilisateur).

## follow-up 21 (carte ne remplissait que ~50% de la case, vraie cause CSS)
Retour utilisateur (2e capture) : la grille est maintenant bien alignée, mais les cartes ne remplissent qu'environ 50% de la hauteur de leur case.
Cause : `.sessiongrid-cell` (`<td>`) posé en `height: auto` — un `<td>` en `auto` ne fournit PAS de hauteur "définie" pour la résolution des `height: 100%` de ses enfants, MÊME UNE FOIS réellement étiré par la ligne du tableau (règle CSS spécifique aux pourcentages, indépendante de la taille réellement rendue). `.sessiongrid-cell-inner { height: 100% }` ne pouvait donc jamais vraiment se résoudre.
Fix : "astuce" CSS bien connue et documentée pour ce cas précis — `height: 1%` au lieu de `auto` sur `.read-only-mode .sessiongrid-cell` (n'importe quelle valeur NON-auto suffit à déclencher la bonne branche de résolution ; `min-height: 60px` reste le vrai plancher, la hauteur réellement rendue vient toujours de l'étirement de la ligne).
tsc+build propres. Pas de suite de tests relancée (100% CSS, accord utilisateur). Toujours pas de vérification visuelle possible ici — à confirmer par l'utilisateur après déploiement.

## follow-up 22 (REVERT complet de l'etirement en hauteur, 3 essais casses)
Retour utilisateur (3e capture) : ligne vide énorme en haut de grille, toujours pas bon — « c'est horrible ce que tu me fais ».
Arrêt des tentatives CSS spéculatives sans vérification visuelle possible. Cause de fond enfin comprise : un `<table>` HTML étiré au-delà de sa hauteur naturelle répartit l'espace en trop ÉGALEMENT entre toutes ses lignes, jamais proportionnellement au contenu — une ligne sans séance devient donc un vide énorme quelle que soit la technique par-dessus (display:flex, height:1%...). Le problème n'était jamais la technique employée, c'est qu'un `<table>` n'a structurellement pas de mécanisme pour ça. Le corriger vraiment demanderait une reconstruction en CSS Grid (pas fait, remis à plus tard si demandé).
REVERT complet : retiré toute la chaîne flex d'étirement (`.app-main`/`.view`/`.panel:last-child`/`.sessiongrid-wrap`/`.sessiongrid`), le hack `height:1%`, le `flex:1` sur `.sessiongrid-block`, et le `font-size: clamp()` qui en dépendait. GARDÉ : la largeur pleine page (`max-width:none`, jamais signalée cassée) et le wrapper `.sessiongrid-cell-inner` (transparent maintenant, utile si CSS Grid un jour). Résultat CSS buildé : taille identique (37.91kB) à celle d'avant tout cet essai — confirme un retour propre à l'état connu-bon.
tsc+build propres. Pas de suite de tests relancée (100% CSS).

## follow-up 23 (salles : edition en Vue Promo, creation hors batiment, audit auth)
Retours utilisateur : « modifier uniquement les salles » en Vue Promo ; « A18 entre parenthèses évaluation... on ne veut pas qu'il y ait écrit évaluation si ce n'est pas une vraie éval » ; « des salles autres que dans le bâtiment... possibilité de créer une salle » ; « /rooms n'est pas protégé, peux-tu vérifier les autres endpoints ? » ; « je n'ai que les cours TP que je peux modifier » ; style de l'input.
1. Libellé `A.018 (Évaluation)` -> `A.018` (rooms.yaml). Le suffixe décrivait la SALLE mais s'affichait dans la case salle du cours, se lisant comme un qualificatif de celui-ci. `room_type: evaluation` (ce qui pilote réellement l'affectation) inchangé. AU PASSAGE : `build_payload` relit désormais le libellé depuis la config courante au lieu du libellé figé dans le placement — sinon renommer une salle n'avait aucun effet sans réaffectation complète.
2. `PATCH /placements/{id}/salle` (nouveau) : change la salle SEULE. Endpoint distinct de `PATCH /placements/{id}` qui refait tous les contrôles de POSITION (ordre pédagogique, PAC/SAE, duo, dispo enseignant) — aucun ne peut changer de verdict à créneau fixe, mais tous peuvent refuser à tort une séance déjà posée à une position limite (ex. placée en forçant), rendant sa salle incorrigible. Conflit de salle calculé DIRECTEMENT (salles combinées + durées incluses) plutôt qu'en filtrant les messages français de `validate_move`, qui casserait à la première reformulation.
3. `POST /rooms` + `api/custom_rooms.py` : salles hors bâtiment, persistées dans `data/state/custom_rooms.json` (volume) et non dans `rooms.yaml` (réécrit à chaque déploiement). Aucune règle d'affectation associée : le solveur ne les choisira jamais seul, elles servent au choix manuel — voulu.
4. SÉCURITÉ : `/rooms` était accessible SANS authentification (trouvé par l'utilisateur). Audit complet des 40 routes : seules `/auth/*` et `/health` étaient publiques, `/rooms` était le seul trou. Corrigé + `require_admin_session` (un lien perso public ne doit pas pouvoir créer des salles). Cause de fond traitée : `_verifier_couverture_auth()` appelé au DÉMARRAGE fait maintenant échouer le lancement si un endpoint n'est ni protégé ni explicitement listé public — l'oubli devient bruyant au lieu d'ouvrir l'accès en silence. Test dédié.
5. BUG « seuls les TP modifiables » : un CM de promo est rendu dans TOUTES les colonnes de sa promo (et sur chaque créneau de sa durée) ; la clé d'édition était `r.id`, donc un `<select autoFocus>` s'ouvrait dans chaque copie, chacun volant le focus au précédent dont l'`onBlur` refermait l'édition. Clé rendue unique par cellule (`${col}-${slot}-${id}`).
6. Modale « Nouvelle salle » : vraie modale interne (pas `window.prompt`, désactivé chez l'utilisateur) + styles d'`<input>` ajoutés (l'app n'avait que des `<select>` stylés, les inputs sortaient bruts).
Vérifié en réel via curl sur le serveur : 404 salle inconnue, changement vers salle libre (créneau strictement inchangé), 409 sur salle occupée, 200 en forçant, création + persistance confirmée APRÈS redémarrage, affectation à la salle créée, puis remise en état complète (base restaurée à l'identique). 11 tests dédiés (`test_changer_salle_2026_08_28.py`) + périmètre mail/auth : 26 passed.
Périmètre de tests volontairement restreint aux fichiers concernés (demande utilisateur : « arrête de lancer tous les tests, 90% n'ont rien à voir »).

## follow-up 24 (avertissement capacite au changement de salle)
Retour utilisateur : « il faut mettre un warning quand l'on change de salle s'il y a un conflit ».
Le conflit d'OCCUPATION déclenchait déjà une confirmation (follow-up 23). Ce qui manquait : la CAPACITÉ — mettre un TD de 30 dans H.006 (15 places) passait totalement en silence, alors que c'est précisément le sujet sur lequel on a passé du temps plus tôt dans la session (capacités réelles des salles de TP ramenées à 15, salles combinées...).
- `changer_salle` calcule l'effectif avec la MÊME fonction que le solveur (`rooms.py::_headcount_for_groups`) pour que l'avertissement dise la même chose que l'affectation automatique.
- Classé en `soft_warnings`, pas `hard_conflicts` : une salle trop petite est presque toujours une erreur mais pas toujours (groupe partiellement absent, TP dédoublé) — on le dit clairement et on laisse trancher, sans décider à la place de l'utilisateur. Reste franchissable via `force`, comme l'occupation.
- Les deux problèmes sont renvoyés ENSEMBLE quand ils coexistent (sinon : corriger l'un, relancer, découvrir l'autre).
- Front : la modale affiche désormais `hard_conflicts` ET `soft_warnings`, avec un titre qui suit ce qui est réellement en cause (« Salle déjà occupée » vs « Attention à la capacité ») — annoncer une occupation pour un simple souci de capacité enverrait chercher un conflit inexistant.
Vérifié en réel (serveur live, vraie séance TD BUT1 de 30 vers H.006) : 409 avec `hard_conflicts: []` et l'avertissement de capacité seul, puis 200 en forçant, puis remise en état. 4 nouveaux tests, 15 passed sur le fichier.

## follow-up 25 (DA du titre, favicon, CACHED, infobulles internes)
Retours utilisateur : « la police du titre ne colle pas du tout à la DA » ; « fait moi un petit favicon » ; « enlève moi ça CACHED » ; « internalise moi les hover, là on est sur les hover du navigateur pour tout ce qui est heures totales des semaines ».
- Typo : `--font-heading` passait par une SERIF (Iowan Old Style/Palatino/Georgia) sur un outil d'admin dense en sans-serif système — rupture de ton. Titres ramenés à la même famille que le corps ; la hiérarchie vient maintenant de la graisse (650) et d'un crénage resserré (-0.015em), pas d'un changement de famille.
- « CACHED » : ce n'est PAS un verdict de solveur mais le statut renvoyé quand le planning vient de la base au lieu d'une résolution fraîche (`api/main.py`, `state.last_status or "CACHED"`). Absent de `STATUS_MAP`, il tombait dans le repli et s'affichait BRUT, en rouge « échec » de surcroît. La pastille n'apparaît désormais que pour un vrai verdict (OPTIMAL/FEASIBLE/INFEASIBLE) — tout statut non reconnu la masque, plutôt que d'exposer du jargon interne.
- Favicon SVG (`frontend/public/favicon.svg`) : grille d'emploi du temps stylisée avec une case « occupée » à l'accent du produit, sur le violet `--accent`. SVG plutôt qu'un jeu de PNG : net à toutes les tailles, rien à maintenir. + `theme-color` pour la barre d'adresse mobile.
- Infobulles de la WeekBar : l'attribut `title` natif (lent, non stylable, invisible au toucher) remplacé par une vraie infobulle interne. `position: fixed` avec coordonnées calculées au survol — la `.weekbar` défile horizontalement, un positionnement relatif se décalerait. Déclenchée aussi au FOCUS clavier (le `title` natif ne l'était qu'au survol) ; `aria-label` conservé pour les lecteurs d'écran.
Vérifié : tsc+build propres, favicon bien servi par le serveur (HTTP 200, image/svg+xml), statut `CACHED` confirmé côté API comme étant bien la valeur qui s'affichait. 100% frontend hors le fichier de session — pas de tests backend relancés (périmètre restreint, demande utilisateur).

## follow-up 26 (favicon invisible : SVG mal forme)
Retour utilisateur : « c'est good mais j'ai pas de favicon ».
Cause RÉELLE trouvée (pas un cache navigateur) : le SVG était mal formé. Un commentaire XML citait le nom du jeton CSS d'accent avec ses DEUX TIRETS de préfixe — or la séquence de deux tirets consécutifs est interdite dans un commentaire XML. Confirmé par `xml.dom.minidom` : `ExpatError: not well-formed, line 4 column 29`, pile sur cette séquence.
Piège du symptôme : le serveur renvoyait bien HTTP 200 avec `image/svg+xml` et le lien `<link rel="icon">` était bien présent dans le HTML servi — tout paraissait correct côté réseau, seul le navigateur refusait silencieusement de rendre l'image, sans erreur visible nulle part. C'est ce qui rendait le diagnostic non évident depuis les seules vérifications HTTP faites au follow-up précédent (j'avais vérifié que le fichier était SERVI, jamais qu'il était VALIDE).
Corrigé + commentaire d'avertissement dans le fichier lui-même pour que le piège ne soit pas réintroduit. Validité désormais vérifiée par parsing réel du fichier SERVI (pas seulement du fichier source).
Pas de repli PNG ajouté : ni PIL ni cairosvg disponibles pour le générer proprement, et un second asset dessiné à la main divergerait du SVG avec le temps. Les navigateurs modernes (Chrome/Firefox/Edge/Safari 16+) gèrent tous le favicon SVG.

## follow-up 27 (donnees privees hors payload public, CI, apercu mail + pixel)
Suite à ma revue produit, l'utilisateur a retenu 1, 2 et 4 (3 = normal, le CM était un test hors prod ; 5 = ne plus y toucher).
1) DONNÉES PERSONNELLES : `/app-state` renvoyait à TOUT lien public les 32 adresses mail ET les contraintes déclarées en texte libre de 30 profs (« mercredi toute la journée »...), alors que ces dernières ne s'affichent qu'en mode admin. Filtrage à la SORTIE de l'endpoint (`_CLES_PRIVEES_PAYLOAD`) : session admin = payload complet, lien public = expurgé de `teacherEmails`/`teachers`/`seancesNonPlacees`/`ruleChecks`/`exceptions`. `teacherLabels` conservé (les noms figurent sur tout emploi du temps, c'est l'objet de l'outil). Vérifié en réel : lien public -> 0 mail / 0 contrainte, mais 2393 séances + 32 noms + 28 semaines + 30 groupes intacts ; session admin inchangée. Les liens restent sans mot de passe, comme demandé.
2) CI : aucune n'existait. `.github/workflows/ci.yml` = tests backend rapides + build frontend (qui inclut `tsc -b`). Mesure des lenteurs : `test_solver.py` (6 tests, ~525 s) et 1 test de cohortes (38 s) concentrent tout -> marqués `slow`, marqueur déclaré dans `pyproject.toml`. Suite rapide = 480 tests en 1 min 47 (contre ~10 min) : assez court pour être réellement regardé à chaque push. La suite complète reste à lancer avant une mise en prod.
   Piège rencontré : mon insertion d'`import pytest` s'était glissée AVANT `from __future__` (erreur de collecte) — corrigé, imports regroupés proprement.
4) APERÇU + SUIVI : `GET /mail/teacher-links/apercu/{code}` rend le mail EXACT via la même fonction que l'envoi (un aperçu calculé autrement finirait par diverger). Affiché dans la modale, par destinataire. Statut PERSISTANT à côté de chaque ligne (« Envoyé le … » / « Lu ✓✓ »), issu du journal, donc survit aux rechargements.
   Pixel de suivi : `GET /mail/pixel/{code}.gif` (GIF 1x1, 43 octets), OBLIGATOIREMENT public — un client mail ne peut présenter aucune session. A nécessité `_PUBLIC_PREFIXES`, testé AVANT `_PROTECTED_PREFIXES` : `/mail/pixel/` tombait sinon sous le préfixe protégé `/mail` et aurait été bloqué (bug attrapé par le garde-fou de couverture auth ajouté au follow-up 23... qui le déclarait « couvert » à tort — j'ai donc aussi corrigé la fonction pour distinguer protégé/public).
   Choix : seule la PREMIÈRE ouverture est enregistrée (accusé de réception, pas surveillance) ; un nouvel envoi remet le compteur à zéro ; une ouverture sans envoi journalisé est ignorée (sinon une URL bricolée fabriquerait de fausses ouvertures). Réserve dite à l'utilisateur : beaucoup de clients bloquent les images, une non-ouverture ne prouve rien.
   Bug de test trouvé au passage : le `TestClient` partagé conservait les cookies, un test « accès public » recevait donc le payload admin — il passait isolément et échouait en suite. Fixture autouse qui vide les cookies avant chaque test.
Vérifié : suite rapide 480 passed / 1 skipped en 1 min 47, build frontend propre, aperçu et pixel testés en réel sur le serveur (cycle envoi -> ouverture -> remontée dans l'annuaire).

## follow-up 28 (base d'integration CELCAT — saisie automatisee sans API)
Contexte utilisateur : Celcat = outil URCA pour les EDT ET la paie des enseignants, SANS API. Ils saisissent aujourd'hui via un autoclicker `.bat` vieillissant (dépend de la résolution d'écran, du PC). Objectif : intégrer ça dans cal-iut, bouton depuis l'interface, onglet dédié, nombre de semaines ajustable, MÉMOIRE pour que re-pousser une semaine déjà envoyée mette à jour au lieu de dupliquer, cadence non robotique. Pas encore d'identifiants (« demain »), demande explicite d'une BASE à adapter.
Analyse des `export/*.bat` fournis : format `ajouter <jour> <hdeb> <hfin> <code_ens> <salle> <code_module> <type> <salle_sans_point> <?>`. Décodé : TD=4, TP=6 ; FLI=20900 confirmé par recoupement avec le CSV. Deux champs non élucidés (le dernier 0/1, et le code CM) -> POSÉS EN QUESTION, pas devinés.
Architecture en 3 couches, pour que l'essentiel soit testable sans navigateur ni identifiants :
- `celcat/mapping.py` : placements -> entrées Celcat (pure traduction). Rien n'est jamais deviné : tout code manquant produit une entrée BLOQUÉE avec son motif exact. Un bloc de 3h donne UNE entrée (l'ancien autoclicker en émettait deux consécutives = 2 séances dans Celcat).
- `celcat/sync.py` : journal `data/state/celcat_sync.json` (volume) associant session_id -> SIGNATURE saisie. Permet de classer chaque séance en créer/modifier/inchangée/supprimer. Bornage par semaine indispensable (sans lui, pousser la semaine 3 proposerait de supprimer tout le reste). Limite ASSUMÉE et documentée : le journal reflète ce que NOUS avons saisi, pas l'état réel de Celcat (aucune lecture possible sans API) — une modif faite à la main là-bas passe inaperçue.
- `celcat/driver.py` : SEUL module nécessitant Celcat. `PiloteSimule` rejoue tout sans navigateur (mode répétition + défaut tant que non configuré) ; `PilotePlaywright` = squelette avec `À ADAPTER` explicites, volontairement NON rempli au jugé (du code qui a l'air fini mais ne marche pas serait pire, sur un outil qui paie les gens). `Rythme` = pauses ALÉATOIRES (une cadence régulière est justement ce qui trahit un script) + pause longue périodique. Échec isolé n'interrompt pas la suite ; interruption vérifiée ENTRE deux séances, jamais au milieu d'un formulaire ; journalisation après CHAQUE saisie pour pouvoir reprendre.
- `data/config/celcat.yaml` : 83 enseignants pré-remplis depuis le CSV (31/32 de nos profs matchés, seul JHU manque), salles avec la particularité H.203 -> H.023, types TD/TP. Modules VIDES : le PDF fourni est SCANNÉ (aucun texte extractible, pas d'OCR ici) — les inventer serait le meilleur moyen de créer des séances fausses.
- `GET /celcat/plan` (session admin) : diagnostic complet sans rien envoyer. `/celcat` ajouté aux préfixes protégés — le garde-fou de couverture auth (follow-up 23) l'a signalé automatiquement, il a servi.
Diagnostic réel sur les 2393 séances : 2393 sans code module, 95 CM sans code de type, 38 avec JHU, 14+14 sur les 2 salles combinées. Aucune autre surprise.
26 tests dédiés, tous verts. Reste : réponses aux 4 questions + l'onglet d'interface (dont le contenu dépend des réponses).

## follow-up 29 (vacataire BTO indisponible les matins de septembre)
Mail transmis (capture) : Barthélémy TOMASINA commence d'habitude en novembre, ne s'attendait pas à des cours en septembre ; INDISPONIBLE tous les matins de septembre, dispo à partir d'octobre, urgence sur « mercredi et jeudi, les 2 premiers cours ».
- Ajouté dans `teacher_availability.yaml` : 22 `forbidden_date_slots` (créneaux 0/1/2), un par jour de cours réel de septembre 2026 — dates DÉRIVÉES du calendrier, pas saisies à la main. Blocage par créneaux et non par journée : ses après-midis restent ouverts, c'est ce qu'il propose lui-même. S'arrête au 30/09 (dispo annoncée dès octobre).
- Diagnostic : 5 séances BTO tombent un matin de septembre. Le serveur n'en signale que 3 — les 2 autres (2 et 3 septembre, PRÉCISÉMENT celles du mail) sont en semaine « current », donc non modifiables même en forçant.
  Cause NON bugguée : `current_relative_week` fait retomber un week-end sur le PROCHAIN lundi enseignable (docstring explicite). Nous sommes samedi 29/08, la semaine du 31/08 est donc déjà « en cours ». Conséquence réelle à signaler : on ne peut pas corriger la semaine qui arrive depuis le week-end qui la précède — exactement le moment où on en a besoin. Décision laissée à l'utilisateur (la règle « une semaine en cours ne se modifie pas » vient de lui).
- Vérifié par recoupement de DEUX méthodes (dates réelles calculées depuis le calendrier vs vérification par le serveur), ce qui est justement ce qui a révélé l'écart de 2 séances. Le premier filtrage par index de semaine était faux (les libellés sont des numéros de département, pas des index solveur).
Salles combinées : la suggestion de l'utilisateur (« un TP dans une salle, le 2e dans l'autre ») ne s'applique PAS — vérifié : h007_h008 contient 14 TD à groupe unique, h201_h203 14 CM de promo. Une séance unique ne peut pas être dans deux salles ; la question reste entière côté Celcat.
Suite rapide : 505 passed.

## follow-up 30 (modules Celcat, deverrouillage semaines, echanges BTO, CM sans salle)
1) MODULES CELCAT : liste fournie en .txt (`export/Liste_modules_MMI.txt`). 71/86 de nos cours ont une correspondance UNIQUE -> écrites dans `celcat.yaml`. NON tranchés, listés en commentaire : 10 AMBIGUS (deux codes Celcat pour le même module — familles « TSB2… vs TSBZ… » et « …0 vs …D », cette dernière avec une coquille dans un libellé qui suggère une entrée obsolète, mais ce n'est qu'une hypothèse) et 5 ABSENTS (WR100BU, WRA307M/313M/319M, WSA501D). Le rapprochement retire le suffixe de parcours (« WR301D » chez nous, « WR301 » chez eux) SEULEMENT en dernier recours, jamais avant l'exact.
2) DÉVERROUILLAGE : `_check_move_editable` et le verrou jumeau de `placer_seance` sont désormais contournables via `force` (demande explicite, marquée comme temporaire « sûrement à remettre par la suite »). Le garde-fou reste actif par défaut. Cas qui l'a rendu nécessaire : un vacataire signale son indisponibilité le samedi pour la semaine du lundi — or le week-end bascule déjà sur cette semaine, qui devient non modifiable au moment précis où il faut la corriger.
3) BTO : 5 séances le matin en septembre. 1 déplacée (BUT3-DEV-FI, 17 créneaux d'après-midi possibles). Les 4 autres (BUT3-DEV-FC alternants) n'avaient que 2 créneaux d'après-midi sur TOUT l'horizon — l'alternance restreint énormément. Sur suggestion de l'utilisateur (« regarder pour interchanger des cours »), 2 ÉCHANGES faits le même jour avec JSA (qui n'a aucune indisponibilité déclarée), dont un sur le MÊME cours WRA507D que BTO assure aussi : BTO 09h30<->JSA 14h00 et BTO 11h00<->JSA 15h30. RÉSULTAT VÉRIFIÉ : 0 séance de BTO le matin en septembre, et les 4 séances échangées valident toutes sans conflit.
4) CM SANS SALLE : le dernier recours de `_pick` rendait « la plus grande salle libre » quel que soit son TYPE — d'où 14 CM de promo dans H.201-203 (deux salles de TD fusionnées). Exception ajoutée pour les CM : plus de dernier recours, la salle reste VIDE (retour utilisateur : elle sera renseignée à la main). Arbitrage INVERSE de celui des TD/TP, et c'est voulu — le coût d'une salle trop petite n'est pas le même pour 15 étudiants que pour une promotion entière. `changer_salle` accepte désormais `room_id: ""` pour retirer une salle. Les 14 CM concernés ont été libérés (vérifié : 0 CM en salle combinée, 14 séances sans salle).
Suite rapide : 505 passed.
