# Corrections manuelles d'enseignant appliquées APRÈS la fusion
# maquette+progression (cf. `ingestion/merge.py::apply_teacher_corrections`).
#
# `data/exports/maquette.json` est récupéré depuis une source distante
# (https://mmi23x02.mmi-troyes.fr/export/maquette, cf. `ingestion/fetch.py`)
# et donc ÉCRASÉ à chaque `cal-iut fetch` : une correction déclarée ici
# survit à un re-fetch, contrairement à un edit direct du JSON récupéré.
#
# `correct_teacher_code` doit déjà exister ailleurs dans le jeu de données
# (nom/prénom résolus par recoupement automatique, jamais ressaisis à la main).
# RETIRÉE le 29/08/2026. Elle inversait WRA304M vers Jérôme Thomas (JTH) au
# motif que la maquette distante se trompait. La situation s'est inversée :
# retour de Kyllian Bresson, « JTH Jérôme Thomas est encore assigné sur le
# module WRA304M, alors qu'il a quitté la formation. C'est ARA Anthony Rageul
# qui reprend ce module ». Vérifié à la source : `contraintes/maquette.json`
# ET `contraintes_update/maquette.json` (octet pour octet identiques) donnent
# tous deux ARA en responsable ET en intervenant. C'était donc CETTE
# correction qui contredisait PREVISIO, pas l'inverse.
#
# Trois séances tombaient sur des créneaux où ARA enseignait déjà : elles ont
# été déplacées AVANT le retrait, pour que le changement n'introduise aucun
# conflit d'enseignant.
teacher_corrections: []
