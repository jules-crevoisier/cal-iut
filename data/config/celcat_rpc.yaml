# Canari 2026-09-01 : UPDATE notes event 202985 ; CREATE sans event_id
# → event_id 1523405 (1 semaine) sur URCA_FORMATION.
# params[0] = tableau d'événements, chaque item a _type_: Event.
# start_time / end_time : objets Date JS (pas d'ISO).
# Create : omettre event_id (0 est lu comme une mise à jour).
methode_ecriture: udlTimetables.save
event_id_create: omit
# Canari 2026-09-05 : suppression du canari 1523412 sur URCA_FORMATION
# (BUT MMI S1 TD AB - 2024, group_id 47925). PAS de méthode dédiée : même
# méthode que la création/modification, avec un enregistrement MINIMAL
# `{"-event_id": <id>, "_type_": "Event"}` (clé au signe moins — convention
# Celcat "supprimer cet id" dans un batch save, pas une valeur négative).
# Vérifié : l'événement disparaît du udlTimetables.load qui suit.
methode_suppression: udlTimetables.save
